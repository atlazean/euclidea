\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{euclidea}

\begin{document}

\begin{tikzpicture}
  \draw[help lines] (0,0) grid (4,4);
  \coordinate (A) at (0,0);
  \coordinate (B) at (3,0);
  \coordinate (C) at (1,2);
  %\coordinate [revolve/angle=60, revolve={A,B}] (C); 
  \tikzset{
    revolve/angle={B,A,C},
  }
  \coordinate [revolve={A,B}] (D); 
  \draw[thick] (A) -- (B) (A) -- (D);
  \foreach \p/\placement in {A/left,B/right,C/above,D/below}{
    \fill[red] (\p) circle (2pt);
    \draw (\p) node[\placement] {$\p$};
  }
\end{tikzpicture}

\end{document}