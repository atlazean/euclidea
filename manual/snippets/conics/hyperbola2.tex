\begin{tikzpicture}
  \draw[help lines] (-5,-5) grid (5,5);

  \coordinate (O) at (0,0);
  \coordinate (A') at (3,2);
  \coordinate (B) at (1,0);

  \path[name path=ray] (O) -- (A');
  
  \hyperbola[draw,thick,domain=-2:2,name path=hyperbola] (1,1);
  \asymptote[draw,dashed,domain=-3:3] (1,1);

  \path[name intersections={of=ray and hyperbola, by=A}];

  \draw[thick,red] (A) -- (O) -- (B);

  \foreach \p/\placement in {O/below, A/above left, B/right}{
    \fill[red] (\p) circle (2pt);
    \draw (\p) node[\placement] {$\p$};
  }
\end{tikzpicture}