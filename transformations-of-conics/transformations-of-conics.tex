% https://www.andrews.edu/~rwright/Precalculus-RLW/Text/07-05.html
\documentclass{standalone}
\usepackage{tikz}

\input{../tikzlibraryeuclidea.code.tex}
\usetikzlibrary{intersections}

\begin{document}

\begin{tikzpicture}
  \tikzset {
    % transformations of ellipse: x^2/9+y^2/3=1
    % translate to (1,2) 
    % conic/define={2,0,6,-4,-24,8}, 
    % translate to (1,2) and rotate around center by 90 deg
    % conic/define={6,0,2,-12,-8,-4},
    % translate to (1,2) and rotate around center by 30 deg
    conic/define={3,-2*sqrt(3),5,-6+4*sqrt(3),-20+2*sqrt(3),5-4*sqrt(3)},
  }
  \axes (-5:5,-5:5);
  \draw[rotate=30] (-5.5,0) -- (5.5,0) (0,-5.5) -- (0,5.5);
  % \draw[purple,shift={(1,2)},rotate=30] (-5.5,0) -- (5.5,0) (0,-5.5) -- (0,5.5);
  \draw[purple,rotate-translate={30:(1,2)}] (-5.5,0) -- (5.5,0) (0,-5.5) -- (0,5.5);
  % \draw[purple,translate-rotate={30:(1,2)}] (-5.5,0) -- (5.5,0) (0,-5.5) -- (0,5.5);
  \path[thick,red,conic,save path=\a];
  \tikzset {
    % transformations of ellipse: x^2/9+y^2/3=1
    % translate to (1,2) 
    % conic/define={2,0,6,-4,-24,8}, 
    % translate to (1,2) and rotate around center by 90 deg
    conic/define={6,0,2,-12,-8,-4},
    % translate to (1,2) and rotate around center by 30 deg
    % conic/define={3,-2*sqrt(3),5,-6+4*sqrt(3),-20+2*sqrt(3),5-4*sqrt(3)},
  }
  \path[thick,red,conic,save path=\b];
  % \draw[thick,teal,translate-rotate={-30:(-1,-2)},use path=\a];
  \draw [thick,use path=\a];
  \draw [thick,use path=\b];
\end{tikzpicture}

\end{document}